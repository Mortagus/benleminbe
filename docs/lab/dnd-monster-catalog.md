# DnD Monster Catalog

This document is kept as a historical note.

The current monster catalog pipeline is documented in:

```text
docs/lab/dnd-bestiary-pipeline.md
```

The current runtime catalog is:

```text
assets/scripts/lab/dnd/bestiary.js
```

Older references to `monster_classes.js`, `monsters.generated.json` and `extract_monsters.php` describe a previous pipeline and should not be used for current work.
