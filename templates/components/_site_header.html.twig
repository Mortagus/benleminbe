{# Components - _site_header.html.twig #}

{% trans_default_domain 'layout' %}

{% set currentRoute = app.request.attributes.get('_route')|default('') %}
{% set siteHeaderNavTemplate = currentRoute starts with 'app_games'
    ? 'shared/navigation/_games_nav.html.twig'
    : (currentRoute starts with 'app_lab'
        ? 'shared/navigation/_lab_nav.html.twig'
        : 'shared/navigation/_pro_nav.html.twig') %}

<header class="site-header" data-site-header>
    <div class="site-header__inner">
        <a href="{{ path('app_home', { _locale: app.request.locale }) }}" class="site-header__brand">
            Benjamin Lemin
        </a>

        <button
            type="button"
            class="site-header__menu-toggle"
            data-menu-toggle
            aria-controls="site-header-nav"
            aria-expanded="false"
            aria-label="{{ 'header.navigation.open_menu'|trans }}"
            data-label-open="{{ 'header.navigation.open_menu'|trans }}"
            data-label-close="{{ 'header.navigation.close_menu'|trans }}"
        >
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
        </button>

        <nav
            id="site-header-nav"
            class="site-header__nav"
            data-site-nav
            aria-label="{{ 'header.navigation.label'|trans }}"
        >
            {% include siteHeaderNavTemplate with { currentRoute: currentRoute } only %}
        </nav>

        <div class="site-header__actions">
            {% include 'components/_lang_switcher.html.twig' %}
            {% include 'components/_theme_switcher.html.twig' %}
        </div>
    </div>
</header>
