<div class="theme-switcher" data-theme-switcher>
    <button
        type="button"
        class="theme-switcher__toggle"
        data-theme-toggle
        aria-label="{{ 'theme.toggle'|trans({}, 'layout') }}"
        aria-expanded="false"
    >
        <span data-theme-icon>◐</span>
    </button>

    <div class="theme-switcher__options">
        <button type="button" data-theme-choice="system" data-theme-icon-value="◐">
            <span class="theme-switcher__label">{{ 'theme.system'|trans({}, 'layout') }}</span>
            <span class="theme-switcher__icon">◐</span>
        </button>

        <button type="button" data-theme-choice="light" data-theme-icon-value="☀">
            <span class="theme-switcher__label">{{ 'theme.light'|trans({}, 'layout') }}</span>
            <span class="theme-switcher__icon">☀</span>
        </button>

        <button type="button" data-theme-choice="dark" data-theme-icon-value="☾">
            <span class="theme-switcher__label">{{ 'theme.dark'|trans({}, 'layout') }}</span>
            <span class="theme-switcher__icon">☾</span>
        </button>
    </div>
</div>
