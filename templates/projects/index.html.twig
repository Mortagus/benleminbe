{% extends 'base.html.twig' %}

{% block title %}{{ 'index.meta_title'|trans({}, 'projects') }}{% endblock %}

{% block body %}
    <main class="projects-page">

        <section class="page-hero">
            <p class="eyebrow">{{ 'index.eyebrow'|trans({}, 'projects') }}</p>
            <h1>{{ 'index.title'|trans({}, 'projects') }}</h1>
            <p class="lead">{{ 'index.lead'|trans({}, 'projects') }}</p>
        </section>

        <section class="projects-list">

            <a href="{{ path('app_projects_delcampe', { _locale: app.request.locale }) }}" class="project-card-link">
                <article class="project-card">
                    <h2>{{ 'index.cards.delcampe.title'|trans({}, 'projects') }}</h2>
                    <p>{{ 'index.cards.delcampe.description'|trans({}, 'projects') }}</p>
                </article>
            </a>

            <a href="{{ path('app_projects_stanhome', { _locale: app.request.locale }) }}" class="project-card-link">
                <article class="project-card">
                    <h2>{{ 'index.cards.stanhome.title'|trans({}, 'projects') }}</h2>
                    <p>{{ 'index.cards.stanhome.description'|trans({}, 'projects') }}</p>
                </article>
            </a>

            <a href="{{ path('app_projects_moveit', { _locale: app.request.locale }) }}" class="project-card-link">
                <article class="project-card">
                    <h2>{{ 'index.cards.moveit.title'|trans({}, 'projects') }}</h2>
                    <p>{{ 'index.cards.moveit.description'|trans({}, 'projects') }}</p>
                </article>
            </a>

        </section>

    </main>
{% endblock %}
